<% Client.all.each do |client| %>
	.screenshot-<%=client.id%> {
		background-image: url('<%=client.image_url %>');
		background-repeat: no-repeat;
		background-position: center;
		background-size: 90%;
		position: fixed;
		z-index: 100000;
	}
<%end%>
