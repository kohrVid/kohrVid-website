- provide(:title, 'Update Client: ' + @client.name.capitalize)

%h1
  Update #{ @client.name }

= render 'clients/form'

%p
  = link_to 'Destroy', @client, method: :delete, data: { confirm: 'Are you sure?' }
  | 

  = link_to 'Back', @client
