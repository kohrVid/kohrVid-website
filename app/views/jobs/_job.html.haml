%li.container-fluid
  .row
    .display-inline.col-lg-3.col-md-3.col-sm-3.col-xs-4
      %strong
        = link_to job.title, job.company_website
      %br
      #{job.start_date.strftime("%b %Y")} - #{job.end_date.strftime("%b %Y")}
    .display-inline.col-lg-6.col-md-7.col-sm-7.col-xs-6
      %br
      = job.description
    .display-inline.col-lg-3.col-md-2.col-sm-2.col-xs-2
      = link_to 'Show', job
      |
      = link_to 'Edit', edit_job_path(job)
      |
      = link_to 'Destroy', job, method: :delete, data: { confirm: 'Are you sure?' }
