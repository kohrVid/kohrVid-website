- provide(:title, 'Jobs')

%h1
  Jobs

%ul.no-bullet.p-l-0
  - @jobs.each do |job|
    = render partial: 'jobs/job', locals: { job: job }

%br
- if admin_is_logged_in
  = link_to 'New Job', new_job_path
